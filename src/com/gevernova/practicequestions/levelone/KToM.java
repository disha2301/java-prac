package com.gevernova.practicequestions.levelone;

import java.io.*;

class KToM {
    public static void main(String[] args) {
        // Distance in kilometers
        double kms = 10.8;

        // Convert kilometers to miles
        double ms = kms * 1.6;

        // Display the result
        System.out.println("The distance " + kms + " km in miles is " + ms);
    }
}
